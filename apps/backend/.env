# Application Configuration
PORT=4000
NODE_ENV=development

# MongoDB Connection
MONGO_URI=mongodb://127.0.0.1:27017/GreenGrow

# JWT Configuration
JWT_SECRET=mygreengrow2024secretkey123456789
JWT_ACCESS_EXPIRES=1800
JWT_REFRESH_EXPIRES_DAYS=14

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=chuanbiradi@gmail.com
SMTP_PASS=jgwe vgct isti rxqx
FROM_EMAIL="GreenGrow "
Ở frontend: sau đăng ký điều hướng sang màn “Kiểm tra email để xác thực” có nút Gửi lại email gọi /auth/verify-email/resend; nếu user cố đăng nhập khi isVerified=false thì chuyển về màn xác thực. Lưu ý bảo mật: chỉ lưu hash của token, không log rawToken ở prod; đặt HTTPS, cấu hình CORS đúng origin; và không để lộ thông tin tồn tại email khi resend (trả thông điệp trung tính kiểu “Nếu email hợp lệ, chúng tôi đã gửi liên kết xác thực”).
# Application URL (Frontend URL)
APP_URL=http://localhost:5173